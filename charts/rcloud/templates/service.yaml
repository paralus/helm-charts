apiVersion: v1
kind: Service
metadata:
  name: {{ include "rcloud.fullname" . }}
  labels:
    {{- include "rcloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rcloud.selectorLabels" . | nindent 4 }}
